version: '3.8'

services:
  archivision-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: archivision-ai
    ports:
      - "3001:3001"  # Exposes the Node Server port 3001
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=3001
      - PUID=99
      - PGID=100
      - TZ=UTC
